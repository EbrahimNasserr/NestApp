
export enum SignatureLevelEnum {
    Bearer = "Bearer",
    System = "System",
  }
  
  export enum TokenTypeEnum {
    Access = "access",
    Refresh = "refresh",
  }
  
  export enum LogOutFlagEnum {
    All = "all",
    Only = "only",
  }
  